import * as errorMessage from './errorMessage.constant';
import * as message from './message.constant';
import * as config from './config.constant'


export {
    message,
    errorMessage,
    config
}
